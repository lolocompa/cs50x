#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    typedef uint8_t BYTE;

    if (argc != 2)
    {
        printf("only 2 command line arguments")
        return 1;
    }

    fopen
}